<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <!-- Main WebView for TikTok -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusable="true"
        android:focusableInTouchMode="true" />

    <!-- Virtual Mouse Cursor Overlay -->
    <ImageView
        android:id="@+id/mouseCursor"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:elevation="20dp"
        android:src="@drawable/ic_cursor"
        android:visibility="gone" />

    <!-- Mode Toast / Status Indicator for TV -->
    <TextView
        android:id="@+id/tvStatusIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="24dp"
        android:background="@drawable/bg_indicator"
        android:elevation="25dp"
        android:text="🖱️ Chế độ chuột: BẬT (Bấm MENU để tắt)"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:visibility="gone" />

    <!-- Loading Indicator -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:elevation="10dp"
        android:indeterminate="true"
        android:visibility="gone" />

    <!-- Error/Offline View -->
    <LinearLayout
        android:id="@+id/errorLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:elevation="10dp"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Không thể tải TikTok. Vui lòng kiểm tra kết nối mạng!"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnRetry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="@color/tiktok_pink"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:text="Thử lại (OK)"
            android:textColor="@color/white" />
    </LinearLayout>

</FrameLayout>
